type Category {
  id: ID! @unique
  name: String!
}

type Video {
  id: ID! @unique
  name: String!
  description: String!
  path: String!
  category: Category!
  isEncoded: Boolean! @default(value: "false")
}
